# Domain docs

This is a single-context repository.

Before changing code, read:

- `CONTEXT.md`
- relevant files under `docs/adr/`

Use project terms from `CONTEXT.md`. Flag conflicts with existing ADRs instead of silently overriding them.
