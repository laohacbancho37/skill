# OpenClaw Skills

Public skills for OpenClaw.

## Skills

- [`config-model-gui-openclaw`](./config-model-gui-openclaw/) — GUI workflow and source for configuring OpenClaw model providers.

## Security

This repository must never contain API keys, tokens, `~/.openclaw/openclaw.json`, runtime logs, or personal configuration. Credentials stay on the user's machine and are read only at runtime when needed.
